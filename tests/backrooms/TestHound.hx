// Unit tests for Hound (CONTRACT §1). Runs under haxe --interp via TestAll.
// SKELETON: no checks yet; run() returns 0 (pass). Returns the number of failed checks.
class TestHound {
    public static function run():Int {
        var fails = 0;
        // SKELETON: contract tests for Hound go here
        return fails;
    }
}
